/* returns the zero-indexed year of MIT given a gregorian year */
function cgtm(gregorianYear){
  return gregorianYear - 1861;
}

/* startYear year of institvte started
 * endYear year of institvte ended
 * size physical area building takes up
 * horizOffset offset on the graph */
function drawBuilding(startYear,endYear,size,horizOffset){
}

/* cgtm(2014) used for 'present' */
/* FILL OUT OTHER VARIABLES LATER */
var b1 = drawBuilding(cgtm(1917),cgtm(2014))
